# LLM provider for browser-use Agent (pick one)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here

# Model to use inside browser-use Agent (auto-defaults per provider)
# BROWSER_USE_MODEL=claude-sonnet-4-20250514

# Browser settings
HEADLESS=true
